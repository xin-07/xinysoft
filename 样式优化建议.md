问题很明确：渐变色提亮后，覆盖在上面的白色半透明效果（网格纹理、光线扫过、浮动装饰）与明亮背景的对比度大幅降低，导致动画"看不见"了。

---

## 原因分析

当前所有叠层效果都使用 **白色半透明** 覆盖在渐变上：

| 效果层 | 当前透明度 | 在 `#60a5fa→#22d3ee` 上的实际表现 |
|---|---|---|
| 网格纹理 | `rgba(255,255,255, 0.025)` | 几乎不可见 |
| 光线扫过 | `rgba(255,255,255, 0.05→0.12)` | 勉强可见 |
| 圆形装饰 | `rgba(255,255,255, 0.05)` | 几乎不可见 |
| 圆环装饰 | `rgba(255,255,255, 0.07)` | 几乎不可见 |
| 光点装饰 | `rgba(255,255,255, 0.18)` | 勉强可见 |

提亮后的渐变亮度已经很高，白色叠加上去等于"白上加白"。

---

## 优化方案

核心思路：**将叠层颜色策略从"白色半透明"改为"深色半透明"**，在明亮渐变上形成可见的对比。同时适当增强动画的幅度和节奏。

### 1. 网格纹理 — 改用深色线条

```css
/* Layer 2: Geometric grid texture (::before) */
.project-card__placeholder::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(rgba(0, 0, 0, 0.08) 1px, transparent 1px),        /* 白 → 深色 */
    linear-gradient(90deg, rgba(0, 0, 0, 0.08) 1px, transparent 1px); /* 白 → 深色 */
  background-size: 32px 32px;
  -webkit-mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.6) 0%, transparent 85%);
  mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.6) 0%, transparent 85%);
  pointer-events: none;
  z-index: 1;
  transition: opacity 0.3s ease;
}

.project-card:hover .project-card__placeholder::before {
  opacity: 0.6;
  background-image:
    linear-gradient(rgba(0, 0, 0, 0.14) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0, 0, 0, 0.14) 1px, transparent 1px);
}
```

深色线条在明亮渐变上反而清晰可见，且不会破坏整体美感。

---

### 2. 光线扫过 — 增强亮度和宽度

```css
/* Layer 4: Diagonal light sweep (::after) */
.project-card__placeholder::after {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: linear-gradient(
    105deg,
    transparent 35%,                          /* 缩窄透明区域，拉宽光带 */
    rgba(255, 255, 255, 0.12) 42%,
    rgba(255, 255, 255, 0.35) 50%,            /* 中心从 0.12 → 0.35 */
    rgba(255, 255, 255, 0.12) 58%,
    transparent 65%
  );
  animation: lightSweep 4s ease-in-out infinite;  /* 5s → 4s，稍快 */
  pointer-events: none;
  z-index: 3;
}

.project-card:hover .project-card__placeholder::after {
  animation-duration: 2s;                      /* 2.5s → 2s */
  background: linear-gradient(
    105deg,
    transparent 35%,
    rgba(255, 255, 255, 0.2) 42%,
    rgba(255, 255, 255, 0.5) 50%,              /* 悬停中心从 0.18 → 0.5 */
    rgba(255, 255, 255, 0.2) 58%,
    transparent 65%
  );
}
```

---

### 3. 浮动装饰 — 提升对比度

```css
/* Layer 5: Floating geometric decorations */
.placeholder-decor {
  position: absolute;
  pointer-events: none;
  z-index: 4;
  opacity: 0.7;                                /* 基础 0.6 → 0.7 */
  transition: opacity 0.3s ease, transform 0.3s ease;
}

.project-card:hover .placeholder-decor {
  opacity: 1;                                  /* 悬停 0.85 → 1 */
}

/* 大圆 — 改用深色半透明，带边框描边增加辨识度 */
.placeholder-decor--circle {
  width: 110px;
  height: 110px;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.06);            /* 白 → 深色 */
  border: 2px solid rgba(0, 0, 0, 0.08);      /* 新增描边 */
  top: -28px;
  right: -18px;
  animation: floatCircle 7s ease-in-out infinite alternate;
}

/* 圆环 — 加粗描边 */
.placeholder-decor--ring {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  border: 3px solid rgba(0, 0, 0, 0.12);      /* 2px 白 → 3px 深色 */
  bottom: 18px;
  left: 14px;
  animation: floatRing 5.5s ease-in-out infinite alternate-reverse;
}

/* 小圆点 — 增大尺寸和发光 */
.placeholder-decor--dot {
  width: 12px;                                 /* 8px → 12px */
  height: 12px;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.15);            /* 白 → 深色 */
  top: 38%;
  right: 22%;
  animation: floatDot 3.5s ease-in-out infinite alternate;
  box-shadow: 0 0 16px 4px rgba(0, 0, 0, 0.1); /* 增强阴影 */
}
```

---

### 4. 首字母 — 增强可读性

当前白色字母在明亮渐变上对比度也不够：

```css
/* Layer 6: First letter */
.project-card__letter {
  position: relative;
  z-index: 5;
  font-size: 2rem;                             /* 1.75rem → 2rem */
  font-weight: 700;
  color: rgba(0, 0, 0, 0.35);                 /* 白色 → 深色 */
  text-shadow:
    0 1px 0 rgba(255, 255, 255, 0.6),         /* 浅色描边增加立体感 */
    0 2px 8px rgba(0, 0, 0, 0.15);
  user-select: none;
  letter-spacing: 0.1em;
  transition: transform 0.3s ease, text-shadow 0.3s ease;
}

/* 字母背后的光晕 — 改用深色径向渐变 */
.project-card__letter::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 90px;                                 /* 80px → 90px */
  height: 90px;
  transform: translate(-50%, -50%);
  background: radial-gradient(
    circle,
    rgba(0, 0, 0, 0.06) 0%,                   /* 白 → 深色 */
    transparent 70%
  );
  pointer-events: none;
  z-index: -1;
  transition: width 0.3s ease, height 0.3s ease;
}

.project-card:hover .project-card__letter {
  transform: scale(1.2);                       /* 1.15 → 1.2 */
  text-shadow:
    0 1px 0 rgba(255, 255, 255, 0.8),
    0 4px 16px rgba(0, 0, 0, 0.2);
}

.project-card:hover .project-card__letter::after {
  width: 120px;                                /* 100px → 120px */
  height: 120px;
}
```

---

### 5. 底部过渡层 — 与优化后一致

保持之前已调整的参数不变：

```css
/* Layer 3: Bottom dark gradient fade */
.placeholder-fade {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 35%;
  background: linear-gradient(
    to top,
    var(--color-surface, #0f3460) 0%,
    rgba(15, 52, 96, 0.25) 50%,
    transparent 100%
  );
  pointer-events: none;
  z-index: 2;
}
```

---

### 6. ProjectBanner.vue 同步修改

Banner 的装饰元素也需要同步调整（当前同样是白色半透明，存在同样的问题）：

```css
/* Banner Layer 2: 网格纹理 */
.banner-placeholder::before {
  background-image:
    linear-gradient(rgba(0, 0, 0, 0.08) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0, 0, 0, 0.08) 1px, transparent 1px);
  background-size: 48px 48px;
}

/* Banner Layer 4: 光线扫过 */
.banner-placeholder::after {
  background: linear-gradient(
    105deg,
    transparent 35%,
    rgba(255, 255, 255, 0.12) 42%,
    rgba(255, 255, 255, 0.35) 50%,
    rgba(255, 255, 255, 0.12) 58%,
    transparent 65%
  );
}

/* Banner Layer 5: 装饰 */
.banner-decor--circle {
  background: rgba(0, 0, 0, 0.06);
  border: 2px solid rgba(0, 0, 0, 0.08);
}

.banner-decor--ring {
  border: 3px solid rgba(0, 0, 0, 0.12);
}

.banner-decor--dot {
  width: 14px;
  height: 14px;
  background: rgba(0, 0, 0, 0.15);
  box-shadow: 0 0 18px 5px rgba(0, 0, 0, 0.1);
}

/* Banner Layer 6: 品牌名 */
.banner-brand {
  font-size: 3.5rem;
  color: rgba(0, 0, 0, 0.35);
  text-shadow:
    0 1px 0 rgba(255, 255, 255, 0.6),
    0 2px 8px rgba(0, 0, 0, 0.15);
}
```

---

## 优化前后效果对比

| 效果层 | 优化前（亮色上白） | 优化后（亮色上暗） |
|---|---|---|
| 网格纹理 | 不可见 | 清晰的细线网格，顶部渐隐 |
| 光线扫过 | 勉强可见 | 明亮的光带扫过，悬停时更宽更亮 |
| 圆形装饰 | 不可见 | 淡暗色圆 + 描边轮廓 |
| 圆环装饰 | 不可见 | 加粗暗色圆环，旋转可见 |
| 光点装饰 | 勉强可见 | 更大的暗色光点，带扩散阴影 |
| 首字母 | 偏淡 | 深色字母 + 浅色描边，悬停放大更明显 |
| 底部过渡 | 已优化 | 保持不变 |

这样在明亮的霓虹渐变上，每层效果都有足够的对比度可感知，动画的"呼吸感"会恢复。同时三种颜色的卡片仍然能一眼区分——网格纹理、光线扫过和浮动装饰都是统一的深色策略，不会抢走品牌渐变色的主角地位。