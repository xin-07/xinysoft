# xinysoft 管理后台 — 前端需求（Vue 3 + Vite）

> 本文档是从 [管理后台需求文档](管理后台需求文档.md) 拆分出的**前端部分**。
> 对应后端需求详见 [管理后台-后端需求.md](管理后台-后端需求.md)。
>
> 前端项目位于：`D:\Project\web\xinysoft_Vite`

## 1. 需求概述

提供一个浏览器端的管理界面，让开发者可以直接在网页上管理项目和个人资料，无需操作数据库。

---

## 2. 前端需求

### 2.1 项目结构新增

```
src/
├── views/admin/
│   ├── AdminLogin.vue        # 登录页面
│   ├── AdminLayout.vue       # 管理后台布局（侧边栏 + 顶栏 + 内容区）
│   ├── AdminDashboard.vue    # 管理后台首页/概览
│   ├── AdminProjects.vue     # 项目管理列表页
│   └── AdminProfile.vue      # 个人资料编辑页
├── components/admin/
│   ├── ProjectForm.vue       # 项目新增/编辑表单
│   ├── ConfirmDialog.vue     # 确认弹窗组件（通用）
│   ├── StatusBadge.vue       # 状态标签组件（已发布/草稿）
│   └── FileUploader.vue      # 文件上传组件
├── composables/
│   └── useAdminAuth.js       # 认证状态管理 composable
├── api/
│   └── admin.js              # 管理后台 API 请求模块
└── router/
    └── index.js              # 追加管理后台路由
```

### 2.2 认证管理

| ID | 需求 | 详细说明 |
|----|------|----------|
| ADM-FE-01 | **`useAdminAuth` composable** | 管理登录状态，包含 `isAuthenticated`（ref）、`token`（ref）、`login()`、`logout()`、`checkAuth()` 方法；Token 存储于 localStorage |
| ADM-FE-02 | **登录页面 `/admin/login`** | 干净简约的登录表单（用户名 + 密码），登录按钮加载态（防止重复提交），错误提示（用户名/密码错误、网络错误），登录成功后跳转 `/admin` |
| ADM-FE-03 | **Token 自动附加** | Axios 请求拦截器自动在 `Authorization` header 附加 Bearer Token |
| ADM-FE-04 | **401 自动退出** | Axios 响应拦截器检测 401 状态码时自动清除 Token 并跳转登录页 |
| ADM-FE-05 | **应用初始化鉴权** | App 启动时通过 `POST /api/admin/verify` 验证 localStorage 中的 Token 是否有效 |

### 2.3 管理后台布局

| ID | 需求 | 详细说明 |
|----|------|----------|
| ADM-FE-06 | **`AdminLayout.vue`** | 管理后台通用布局组件：左侧窄侧边栏（导航菜单）、顶部状态栏（当前用户标识 + 退出按钮）、右侧内容区 |
| ADM-FE-07 | **侧边栏导航** | 菜单项：概览、项目管理、个人资料、留言管理（P1 预留），当前菜单高亮，底部显示退出按钮 |
| ADM-FE-08 | **视觉风格** | 复用现有 CSS 变量（`--color-surface`、`--color-accent` 等），与管理后台风格一致，但布局为独立的两栏结构 |
| ADM-FE-09 | **响应式** | 桌面端侧边栏常驻（~240px），移动端侧边栏收起为汉堡菜单 |

### 2.4 项目列表页 `/admin/projects`

| ID | 需求 | 详细说明 |
|----|------|----------|
| ADM-FE-10 | **项目表格展示** | 表格列：序号、标题、技术栈（缩略标签）、状态（已发布/草稿）、排序权重、创建时间、操作按钮 |
| ADM-FE-11 | **技术栈标签缩略** | 技术栈多于 3 个时显示 `Vue3, FastAPI, +2` 格式 |
| ADM-FE-12 | **状态切换** | 行内一键切换发布/草稿状态，使用 Toggle 或按钮，切换后即时反馈 |
| ADM-FE-13 | **删除确认** | 点击删除后弹出确认对话框，显示项目标题以防误删 |
| ADM-FE-14 | **新增按钮** | 页面右上角"新增项目"按钮，跳转到新增表单 |
| ADM-FE-15 | **编辑入口** | 每行"编辑"按钮，跳转到编辑表单 |
| ADM-FE-16 | **加载/空/错误状态** | 列表加载中显示骨架屏，无数据显示空状态提示，接口报错显示错误提示+重试按钮 |
| ADM-FE-17 | **分页** | 列表底部分页控件，支持页码切换和每页条数选择 |

### 2.5 项目表单（新增/编辑）

| ID | 需求 | 详细说明 |
|----|------|----------|
| ADM-FE-18 | **`ProjectForm.vue`** | 同一组件处理新增和编辑，通过 `projectId` prop 区分模式 |
| ADM-FE-19 | **表单字段** | 标题（必填）、副标题、描述（textarea）、技术栈（标签输入组件，支持回车添加/×删除）、封面 URL（FileUploader）、截图 URL 列表（FileUploader）、线上链接、仓库链接、精选开关、排序权重（数字输入） |
| ADM-FE-20 | **文件上传组件 `FileUploader.vue`** | 支持拖拽/点击上传，上传成功后展示缩略图预览和 URL；已有 URL 可直接输入，不上传时也可手动填写 URL |
| ADM-FE-21 | **技术栈标签输入器** | 输入框 + 回车添加标签，每个标签可点击 × 移除，标签显示为彩色 pill 样式（复用 `techStackColors`） |
| ADM-FE-22 | **表单校验** | 标题必填（提交前校验）、描述最大长度限制（如适用）、URL 格式校验（可选） |
| ADM-FE-23 | **保存反馈** | 保存成功显示 Toast 提示并跳转回列表页，保存失败显示错误信息 |
| ADM-FE-24 | **取消/返回** | 表单底部"取消"按钮返回列表页，提示"未保存的更改将丢失"（dirty 检测） |
| ADM-FE-25 | **编辑预填充** | 编辑模式下从 API 获取项目详情并预填表单 |

### 2.6 个人资料编辑页 `/admin/profile`

| ID | 需求 | 详细说明 |
|----|------|----------|
| ADM-FE-26 | **表单字段** | 姓名、头衔、个人简介（textarea）、技术标签（同项目表单的标签输入器）、GitHub 链接、Gitee 链接、微信、QQ、邮箱列表（标签输入器，按回车添加）、头像上传（FileUploader） |
| ADM-FE-27 | **数据加载** | 进入页面时自动加载当前个人资料数据并预填 |
| ADM-FE-28 | **保存反馈** | 保存成功 Toast 提示，首页刷新后可见变更 |
| ADM-FE-29 | **取消按钮** | 返回管理后台首页 |

### 2.7 管理后台首页 `/admin`

| ID | 需求 | 详细说明 |
|----|------|----------|
| ADM-FE-30 | **概览信息** | 显示项目总数、已发布数、草稿数、留言总数（P1）等简单统计 |
| ADM-FE-31 | **快速入口** | 快速导航到"新增项目"、"编辑个人资料"等常用操作 |

### 2.8 路由与守卫

| ID | 需求 | 详细说明 |
|----|------|----------|
| ADM-FE-32 | **管理后台路由** | 嵌套路由统一挂载在 `/admin` 下，置于 `AdminLayout.vue` 的子路由中 |
| ADM-FE-33 | **路由守卫** | Vue Router `beforeEach` 导航守卫检查 `/admin/*` 路径是否已认证，未认证跳转 `/admin/login` |
| ADM-FE-34 | **登录页特殊处理** | 已登录用户访问 `/admin/login` 时自动跳转 `/admin` |
| ADM-FE-35 | **路由懒加载** | 所有管理后台页面使用 `() => import(...)` 延迟加载 |

```
路由结构：
/admin               → AdminLayout
  /admin             → AdminDashboard（概览首页）
  /admin/projects    → AdminProjects（项目列表）
  /admin/projects/new → ProjectForm（新增项目）
  /admin/projects/:id/edit → ProjectForm（编辑项目）
  /admin/profile     → AdminProfile（个人资料编辑）
  /admin/messages    → [P1 预留]
/admin/login         → AdminLogin（独立布局，不嵌入 AdminLayout）
```

### 2.9 API 模块

| ID | 需求 | 详细说明 |
|----|------|----------|
| ADM-FE-36 | **`src/api/admin.js`** | 独立的 API 模块，封装所有管理接口的 Axios 请求 |
| ADM-FE-37 | **请求/响应拦截** | `src/api/index.js` 中新增请求拦截器（加 Token）和响应拦截器（401 自动退出） |

```javascript
// api/admin.js 导出函数示例
export const adminAPI = {
  login(username, password),
  verify(),
  getProjects(page, pageSize),
  getProject(id),
  createProject(data),
  updateProject(id, data),
  deleteProject(id),
  toggleStatus(id, status),
  getProfile(),
  updateProfile(data),
  uploadFile(file)
}
```

### 2.10 前端工作量评估

| 模块 | 预估时间 |
|------|---------|
| 登录页面 + useAdminAuth composable | 3-4 小时 |
| AdminLayout 布局 + 侧边栏 | 2-3 小时 |
| 项目列表页（表格 + 分页 + 状态切换 + 删除） | 4-6 小时 |
| 项目新增/编辑表单（ProjectForm + FileUploader + 标签输入器） | 6-8 小时 |
| 个人资料编辑页 | 2-3 小时 |
| 路由配置 + 导航守卫 | 1 小时 |
| 管理后台首页/概览 | 1-2 小时 |
| API 模块 + 拦截器 | 1 小时 |
| **合计** | **20-28 小时** |

---

## 3. 前后端对接约定

### 3.1 统一响应格式

所有管理接口遵循现有 `ApiResponse` 格式：

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

### 3.2 认证 Header

```
Authorization: Bearer <jwt_token>
```

### 3.3 错误码约定

| HTTP 状态码 | 说明 | 前端处理 |
|------------|------|---------|
| 200 | 成功 | 正常处理 |
| 400 | 请求参数校验失败 | 显示具体错误信息 |
| 401 | Token 无效/过期 | 清除 Token，跳转登录页 |
| 403 | 无权限 | 显示无权限提示 |
| 404 | 资源不存在 | 显示 404 提示 |
| 429 | 登录频率限制 | 显示"操作过于频繁，请稍后再试" |
| 500 | 服务器错误 | 显示"服务器错误，请稍后重试" |

### 3.4 开发联调流程

```
1. 后端先完成 JWT 认证接口，提供 Swagger UI 测试
2. 前端开发登录页面并串联认证流程
3. 后端完成项目 CRUD + 个人资料接口
4. 前端开发项目管理页面并串联接口
5. 端到端验证：登录 → 创建项目 → 编辑 → 发布 → 验证首页展示
```

---

## 4. 工作量评估

| 模块 | 预估时间 |
|------|---------|
| **前端**（登录 + 项目管理 + 个人资料 + 布局） | 20-28 小时 |
| **联调测试** | 3-5 小时 |
| **合计** | **23-33 小时** |

---

## 5. 相关文档

- [管理后台需求文档](管理后台需求文档.md) — 完整版需求文档
- [管理后台-后端需求.md](管理后台-后端需求.md) — 后端需求
- [项目功能迭代需求分析文档](项目功能迭代需求分析文档.md) — 完整功能路线图

## 6. 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| 1.0 | 2026-06-11 | 初始版本，从管理后台需求文档拆分出前端部分 |
