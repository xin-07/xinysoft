# xinysoft 个人作品集 — 功能迭代需求分析

## 1. 项目概述

### 1.1 项目简介

xinysoft 是一个个人作品集项目，包含两个部分：

| 模块 | 技术栈 | 用途 |
|------|--------|------|
| **前端** | Vue 3 + Vite | 个人作品集 SPA 网站 |
| **后端** | FastAPI + MySQL | 提供 REST API 数据服务 |

### 1.2 当前已实现功能

**前端（Vue 3 + Vite）：**

| 功能 | 说明 |
|------|------|
| 个人主页 / Hero 区域 | 显示姓名、头衔、个人简介、技术标签、社交链接 |
| 项目列表页 `/projects` | 分页网格展示，含加载/错误/空三态处理 |
| 项目详情页 `/projects/:id` | Banner、技术栈标签、描述、线上链接 |
| 精选项目组件 | 首页展示精选项目 |
| 深色/浅色主题切换 | localStorage 持久化 + 系统偏好检测 + 防闪白 |
| 联系信息弹窗 | 展示邮箱、微信、QQ，支持点击复制 |
| 粒子边框动画 | Canvas 粒子动画，含 FPS 降级保护 |
| 页面过渡动画 | 路由切换淡入效果 |
| 响应式布局 | 适配 480px ~ 1920px 断点 |

**后端（FastAPI + MySQL）：**

| 接口 | 说明 |
|------|------|
| `GET /api/profile` | 获取个人资料（数据库异常时返回硬编码默认数据） |
| `GET /api/avatar/{path}` | 头像文件服务（白名单目录 + 路径遍历防护） |
| `GET /api/projects` | 项目列表（分页、精选筛选） |
| `GET /api/projects/{id}` | 项目详情 |

**数据库（MySQL 8.4.9）：**

| 表 | 说明 |
|----|------|
| `profile` | 个人信息，单行记录 |
| `projects` | 项目作品集，当前 3 条种子数据 |

---

## 2. 核心问题分析

### 2.1 最大痛点：无内容管理界面

当前修改任何内容都必须直接操作 MySQL：

| 操作 | 当前方式 | 问题 |
|------|---------|------|
| 添加新项目 | 写 SQL INSERT，手拼 JSON | 繁琐、易出错 |
| 修改个人简介 | 写 SQL UPDATE | 不够直观 |
| 发布/下线项目 | 手动改 `status` 字段 | 无操作界面 |
| 调整排序 | 改 `sort_order` 字段 | 无法拖拽调整 |
| 查看访客留言 | 不存在此功能 | 无渠道 |

### 2.2 次要问题

| 问题 | 说明 |
|------|------|
| 无搜索过滤 | 项目数量增长后无法按技术栈筛选 |
| 联系渠道单一 | 只展示联系方式，访客无法主动发消息 |
| 无博客系统 | 缺少技术文章分享和 SEO 内容积累 |
| SEO 薄弱 | 仅 document.title，无 Open Graph / JSON-LD |
| 无测试 | 后端无测试文件，迭代风险高 |

---

## 3. 功能需求

### 3.1 P0 — 管理后台（Admin Dashboard）

#### 3.1.1 需求描述

提供一个浏览器端的管理界面，让开发者可以直接在网页上管理项目和个人资料，无需操作数据库。

管理后台分前、后端独立开发：

- **后端（FastAPI + MySQL）**：位于 `D:\Project\python_project\xinysoft_FastAPI`，提供管理员认证鉴权、项目 CRUD、个人资料编辑等 REST API
- **前端（Vue 3 + Vite）**：位于 `D:\Project\web\xinysoft_Vite`，提供登录页、项目管理页、个人资料编辑页等 SPA 界面

#### 3.1.2 详细需求

> 完整的后端需求（认证系统、项目管理 CRUD、个人资料管理、文件上传、安全性要求、接口一览、工作量评估）和前端需求（项目结构、认证管理、后台布局、项目列表/表单、个人资料编辑、路由守卫、API 模块、工作量评估），以及前后端对接约定（响应格式、错误码、联调流程）请见独立文档：**[管理后台需求文档](管理后台需求文档.md)**。

#### 3.1.3 工作量评估（概要）

| 模块 | 预估时间 |
|------|---------|
| **后端**（认证 + CRUD + 文件上传 + 测试） | 12-19 小时 |
| **前端**（登录 + 项目管理 + 个人资料 + 布局） | 20-28 小时 |
| **联调测试** | 3-5 小时 |
| **合计** | **35-52 小时** |

---

### 3.2 P1 — 技术栈搜索与过滤

#### 3.2.1 需求描述

在项目列表页增加按技术栈筛选功能，让访客可以快速找到感兴趣的项目。

#### 3.2.2 详细需求

| ID | 需求 | 说明 |
|----|------|------|
| FIL-01 | 后端支持 `?tech=Vue3` 查询参数 | 使用 `JSON_CONTAINS` 在 `tech_stack` JSON 字段中匹配 |
| FIL-02 | 项目列表页上方显示技术栈标签列表 | 每个标签可点击切换筛选 |
| FIL-03 | 标签从所有已发布项目中动态提取 | 聚合去重后展示 |
| FIL-04 | 筛选状态同步到 URL | `/projects?tech=Vue3`，支持分享和浏览器返回 |
| FIL-05 | 已选标签高亮 | 活跃筛选标签有视觉区分 |
| FIL-06 | 清除筛选按钮 | 一键清除所有筛选条件 |
| FIL-07 | 筛选结果为空时显示空状态 | "没有匹配的项目" + 清除筛选按钮 |

#### 3.2.3 接口变更

| 变更 | 说明 |
|------|------|
| `GET /api/projects` 新增参数 | `tech: str = None`，按技术栈筛选 |
| `GET /api/projects/tech-stacks`（新增） | 返回所有已发布项目使用的技术栈列表（聚合去重） |

#### 3.2.4 工作量评估

| 模块 | 预估时间 |
|------|---------|
| 后端接口修改 + 新增 | 0.5 天 |
| 前端筛选 UI | 0.5 天 |
| **合计** | **1 天** |

---

### 3.3 P1 — 联系表单

#### 3.3.1 需求描述

在联系弹窗中增加"发消息"功能，让访客可以直接通过网站发送留言，无需复制邮箱地址。

#### 3.3.2 详细需求

| ID | 需求 | 说明 |
|----|------|------|
| CON-01 | `POST /api/contact` 后端接口 | 接收姓名、邮箱、消息内容 |
| CON-02 | 新建 `contact_messages` 表 | 字段：id, name, email, message, created_at, is_read |
| CON-03 | 表单验证 | 姓名必填、邮箱格式验证、消息必填且不超过 1000 字 |
| CON-04 | 速率限制 | 同一 IP 15 分钟内仅可提交 1 次（内存 dict 实现） |
| CON-05 | 前端联系弹窗新增"发消息"标签页 | 与现有联系方式 Tabs 切换 |
| CON-06 | 提交反馈 | 成功 Toast / 错误提示 / 表单重置 |
| CON-07 | 管理后台查看留言 | 列表展示所有留言，标记已读/未读（P1 阶段在前端 `/admin/messages` 页面实现，对应后端 `GET /api/admin/contact-messages` 接口） |

#### 3.3.3 工作量评估

| 模块 | 预估时间 |
|------|---------|
| 后端新接口 + 数据库表 | 0.5 天 |
| 前端弹窗标签页 | 0.5 天 |
| 管理后台留言管理（如阶段 1 已实现） | 0.5 天 |
| **合计** | **1-1.5 天** |

---

### 3.4 P2 — 博客引擎

#### 3.4.1 需求描述

增加博客系统，支持发布技术文章，提升个人品牌和 SEO 效果。

#### 3.4.2 详细需求

| ID | 需求 | 说明 |
|----|------|------|
| BLOG-01 | 新建 `blog_posts` 表 | 字段：id, title, slug, content, excerpt, cover_url, tags, status, published_at, created_at, updated_at |
| BLOG-02 | 公开接口 | `GET /api/blog`（列表，含分页和标签过滤）、`GET /api/blog/{slug}`（详情） |
| BLOG-03 | 管理接口 | 管理后台 CRUD（复用阶段 1 的管理后台） |
| BLOG-04 | 博客列表页 `/blog` | 卡片列表，显示标题、摘要、标签、发布日期 |
| BLOG-05 | 博客详情页 `/blog/:slug` | Markdown 渲染（使用 `marked` 轻量库）、目录生成 |
| BLOG-06 | 标签过滤 | 点击标签筛选同类文章 |
| BLOG-07 | 分页 | 列表分页加载 |
| BLOG-08 | URL 设计 | 使用语义化 slug，如 `/blog/getting-started-with-fastapi` |

#### 3.4.3 工作量评估

| 模块 | 预估时间 |
|------|---------|
| 后端接口 + 数据库 | 1-2 天 |
| 前端列表页 + 详情页 | 2-3 天 |
| 管理后台集成 | 0.5 天 |
| **合计** | **3.5-5.5 天** |

---

### 3.5 P2 — SEO 元数据增强

#### 3.5.1 需求描述

增加动态元数据标签，提升社交媒体分享预览效果和搜索引擎友好度。

#### 3.5.2 详细需求

| ID | 需求 | 说明 |
|----|------|------|
| SEO-01 | 动态 `<title>` | 每个页面有独立标题（已有 `document.title` 更新，不做变动） |
| SEO-02 | 动态 `<meta name="description">` | 每个页面有独立描述 |
| SEO-03 | Open Graph 标签 | `og:title`、`og:description`、`og:image`、`og:url` |
| SEO-04 | JSON-LD 结构化数据 | `WebSite`、`Person`、`Project` 结构化标记 |
| SEO-05 | 动态 `sitemap.xml` | 包含项目详情页链接 |

#### 3.5.3 技术方案

使用 `@unhead/vue` 库实现声明式 head 管理，无需 SSR，在页面组件中声明 meta 信息：

```javascript
// 在页面组件中
useHead({
  title: () => `${project.value.title} - xinysoft`,
  meta: [
    { name: 'description', content: project.value.subtitle },
    { property: 'og:title', content: project.value.title },
    { property: 'og:description', content: project.value.subtitle },
  ],
})
```

#### 3.5.4 工作量评估

| 模块 | 预估时间 |
|------|---------|
| 安装配置 `@unhead/vue` + 全局 SEO | 0.5 天 |
| 逐页面配置 meta 标签 | 0.5 天 |
| JSON-LD + sitemap.xml | 0.5 天 |
| **合计** | **1.5 天** |

---

## 4. 实施路线图

### 4.1 阶段划分

| 阶段 | 功能 | 预估工期 | 核心目标 |
|------|------|---------|---------|
| **阶段 1** | 管理后台（认证 + 项目 CRUD + 个人资料编辑） | 2 周 | 消除最大痛点，告别手写 SQL |
| **阶段 2** | 技术栈筛选 + 联系表单 | 1 周 | 改善访客体验，增加互动渠道 |
| **阶段 3** | 博客引擎 + SEO 增强 | 2-3 周 | 增加内容深度和可发现性 |

### 4.2 阶段 1 详细任务

```
周 1（后端）:
  ├── 环境准备：安装 python-jose、passlib、python-multipart 依赖
  ├── JWT 认证：admin 用户配置（环境变量）、登录接口、Token 验证接口
  ├── 认证中间件：FastAPI Depends() 保护所有 /api/admin/ 路由
  ├── 项目 CRUD 接口：列表（含草稿）、详情、新增、编辑、删除、状态切换
  ├── 个人资料编辑接口 + 文件上传接口
  └── 验证：Swagger UI 测试所有接口，认证拦截正常

周 2（前端）:
  ├── useAdminAuth composable + API 请求模块 + 拦截器配置
  ├── 登录页面 /admin/login（简约表单 + 错误处理）
  ├── AdminLayout 布局组件（侧边栏 + 内容区）
  ├── 项目列表页（表格 + 分页 + 状态切换 + 删除确认）
  ├── 项目新增/编辑表单（ProjectForm + FileUploader + 标签输入器）
  ├── 个人资料编辑页
  ├── 路由配置 + 导航守卫 + 路由懒加载
  └── 验证：端到端流程，从登录到发布项目
```

### 4.3 阶段 2 详细任务

```
  ├── 后端：?tech= 查询参数 + /api/projects/tech-stacks 接口
  ├── 前端：项目页技术栈标签过滤器
  ├── 后端：POST /api/contact + contact_messages 表
  ├── 前端：联系弹窗"发消息"标签页
  └── 验证：筛选正常 / 留言可入库可查看
```

### 4.4 阶段 3 详细任务

```
  ├── 后端：blog_posts 表 + 公开接口 + 管理接口
  ├── 前端：博客列表页 + 详情页（Markdown 渲染）
  ├── 前端：管理后台集成博客编辑
  ├── 前端：@unhead/vue 动态 meta 标签
  ├── 前端：JSON-LD 结构化数据
  └── 验证：发布文章 → 分享到社交媒体 → 预览卡片正常
```

---

## 5. 架构与设计约束

### 5.1 认证方案

- **方案**：JWT（python-jose + bcrypt），不引入 OAuth / SSO
- **后端**：
  - 管理员凭据通过环境变量 `ADMIN_USERNAME` + `ADMIN_PASSWORD_HASH`（bcrypt）配置
  - JWT 密钥通过环境变量 `JWT_SECRET_KEY` 配置
  - Token 有效期 7 天
  - FastAPI `Depends(get_current_admin)` 中间件保护所有管理路由
- **前端**：
  - Token 存储于 localStorage
  - `useAdminAuth` composable 管理登陆状态
  - Axios 请求拦截器自动附加 `Authorization: Bearer <token>`
  - Axios 响应拦截器检测 401 自动清除 Token 并跳转登录页
- **适用性**：个人项目，单一管理员，不需要复杂的权限体系

### 5.2 前端状态管理

- 不使用 Pinia / Vuex
- 认证状态：`useAdminAuth` composable（ref + localStorage）
- 页面数据：组件内 Composition API 管理（与现有模式一致）
- 后续博客等功能复用相同模式

### 5.3 打包策略

- 管理后台页面延迟加载：`() => import('../views/admin/AdminProjects.vue')`
- 博客页面延迟加载
- 初始包体积控制在 70KB 以内

### 5.4 数据库扩展

- 按功能模块递增：`contact_messages` → `blog_posts`
- 使用递增 SQL 文件记录变更：`migrations/001_create_contact_messages.sql`
- 现有表结构不做破坏性修改

### 5.5 安全考虑

| 场景 | 措施 |
|------|------|
| 认证 | JWT Token，密码 bcrypt 加密存储 |
| 管理接口防护 | FastAPI Depends() 认证依赖 |
| XSS | Vue 默认转义，管理端输入清洗 |
| SQL 注入 | 现有参数化查询，新增接口沿用 |
| CORS | 开发环境 `*`，生产环境限制具体域名 |

### 5.6 视觉一致性

- 管理后台复用现有 CSS 变量（`var(--color-surface)`、`var(--color-accent)` 等）
- 使用 `AdminLayout.vue` 包装管理页面，包含侧边栏和面包屑导航
- 不是独立主题，而是网站功能的延伸

---

## 6. 不建议在当前阶段实施的功能

| 功能 | 不建议的原因 |
|------|-------------|
| Pinia / Vuex 状态管理 | 当前架构不需要，认证状态用 composable 足够 |
| SSR / SSG | 个人作品集 SPA 足够，`@unhead/vue` 满足分享需求 |
| GitHub 项目自动同步 | API 限制多、收益小，管理后台手动维护更可靠 |
| CI/CD 流水线 | 功能完善后再配置不迟 |
| 评论系统 | 嵌入第三方方案（如 giscus）即可，无需自建 |
| Docker 容器化 | 单机部署，MySQL 直连，容器化带来的复杂度 > 收益 |
| AWS Lambda 部署 | mangum 已引入但暂时无用，功能优先于基础设施 |

---

## 7. 验证方式

### 7.1 阶段 1 验证

| 验证项 | 方法 |
|--------|------|
| **后端认证** | Swagger UI 测试 `POST /api/admin/login`，错误密码应返回 401 |
| **后端项目 CRUD** | Swagger UI 逐一测试项目管理接口（列表、新增、编辑、删除、状态切换） |
| **后端鉴权保护** | 不带 Token 请求 `/api/admin/*` 任一接口应返回 401 |
| **文件上传** | `POST /api/admin/upload` 上传图片，确认返回可访问的 URL |
| **前端登录** | 访问 `/admin/login`，输入错误密码应提示错误，正确密码应跳转 `/admin` |
| **前端项目管理** | 登录后创建项目 → 编辑 → 切换草稿/发布 → 删除，每一步界面反馈正常 |
| **前端个人资料编辑** | 修改简介和社交链接 → 保存 → 刷新首页确认生效 |
| **前端权限保护** | 未登录直接访问 `/admin/projects` 应跳转登录页 |

### 7.2 阶段 2 验证

| 验证项 | 方法 |
|--------|------|
| 技术栈筛选 | 访问 `/projects?tech=Vue3`，只显示匹配项目 |
| 联系表单 | 提交表单 → 检查 `contact_messages` 表是否有新记录 |
| 速率限制 | 同一 IP 重复提交应被拒绝 |

### 7.3 阶段 3 验证

| 验证项 | 方法 |
|--------|------|
| 博客 | 发布文章 → 访问 `/blog/getting-started-with-fastapi` 确认渲染 |
| SEO | 分享链接到微信/QQ，确认有预览卡片 |

---

## 8. 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| 1.0 | 2026-06-11 | 初始版本，基于首次项目分析产出 |
| 1.1 | 2026-06-11 | 管理后台需求拆分为前端（Vue 3）和后端（FastAPI）独立需求，补充详细任务清单 |
