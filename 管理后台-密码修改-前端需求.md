# xinysoft 管理后台 — 密码修改功能 · 前端需求（Vue 3 + Vite）

> **关联文档**：[管理后台-密码修改-后端需求.md](../../python_project/xinysoft_FastAPI/管理后台-密码修改-后端需求.md)
>
> 前端项目位于：`D:\Project\web\xinysoft_Vite`
>
> 本文档作为现有[管理后台-前端需求.md](管理后台-前端需求.md)的增量补充。

---

## 1. 需求概述

在管理后台中增加**管理员自助修改密码**功能，允许已登录的管理员通过界面修改自己的登录密码，无需操作数据库。

**设计原则：**
- 安全敏感操作与普通资料编辑分离，独立页面展示
- 表单交互清晰，状态反馈及时
- 遵循现有管理后台的 UI 风格和组件体系

---

## 2. 前端需求

### 2.1 新增页面

| 属性 | 值 |
|------|-----|
| **页面路径** | `/admin/password` |
| **页面组件** | `src/views/admin/AdminPassword.vue` |
| **路由名称** | `AdminPassword` |
| **页面标题** | `管理后台 — 安全设置` |
| **布局** | 复用 `AdminLayout.vue`（作为子路由） |

### 2.2 新增路由

在 `src/router/index.js` 的管理后台子路由中添加：

```javascript
{
  path: 'password',
  name: 'AdminPassword',
  component: () => import('../views/admin/AdminPassword.vue'),
  meta: { title: '管理后台 — 安全设置' }
}
```

### 2.3 侧边栏导航更新

在 `AdminLayout.vue` 的 `navItems` 数组中新增菜单项：

```javascript
{ name: 'AdminPassword', path: '/admin/password', label: '安全设置', icon: '&#x1F512;' }
```

建议插入位置：`个人资料` 之后、`退出登录` 之前。

### 2.4 表单设计

| 字段 | 标签 | 类型 | 校验规则 |
|------|------|------|----------|
| `current_password` | 当前密码 | password（可切换明文/密文显示） | 必填 |
| `new_password` | 新密码 | password（可切换明文/密文显示） | 必填，至少 8 位 |
| `confirm_password` | 确认新密码 | password（可切换明文/密文显示） | 必填，必须与 `new_password` 一致 |

**额外 UI 元素：**
- 新密码输入框下方显示密码强度指示器（弱/中/强）
- 每个密码框右侧有一个"显示/隐藏"切换图标（眼睛图标）

### 2.5 密码强度规则

| 强度 | 条件 |
|------|------|
| **弱** | 仅字母或仅数字，长度 < 8 |
| **中** | 字母 + 数字，长度 ≥ 8 |
| **强** | 字母 + 数字 + 特殊字符，长度 ≥ 10 |

> 密码强度仅为前端视觉提示，不影响提交。后端会做独立的长度校验（≥ 8 位）。

### 2.6 页面状态

| 状态 | 表现 |
|------|------|
| **默认态** | 表单为空，所有输入使用 `v-model` 绑定，提交按钮置灰（disabled），准备就绪后启用 |
| **加载态** | 提交按钮显示 `admin-btn--loading` 类（旋转动画），按钮文字变为"修改中..."，所有输入框禁用 |
| **成功态** | Toast 提示"密码修改成功"，表单全部重置为空，保留在当前页面 |
| **错误态** | Toast 显示后端返回的错误信息（如"当前密码错误"），不清空已填字段，方便用户修改后重试 |
| **校验失败** | 输入框下方显示红色提示文字（如"两次密码不一致"、"密码至少 8 位"），不提交 |

### 2.7 组件结构

**文件位置：** `src/views/admin/AdminPassword.vue`

```vue
<script setup>
// 依赖：
//   - adminAPI（来自 src/api/admin.js）
//   - useToast（来自 src/composables/useToast.js）
//   - useRouter / useRoute（Vue Router）

// 状态：
//   - form: { current_password, new_password, confirm_password }
//   - showPassword: { current: false, new: false, confirm: false }  // 切换明文/密文
//   - submitting: ref(false)  // 提交中

// 方法：
//   - validateForm() → boolean  // 前端校验
//   - handleSubmit()  // 调用 API
//   - handleCancel()  // 返回上一页或管理首页
//   - resetForm()     // 重置表单
//   - getPasswordStrength(password) → 'weak' | 'medium' | 'strong'
</script>

<template>
  <!-- 页面标题: "安全设置" 或 "修改密码" -->
  <!-- 表单卡片，与 AdminProfile.vue 风格保持一致 -->
  <!-- 输入组: 当前密码 / 新密码（+强度指示器）/ 确认新密码 -->
  <!-- 操作按钮: 取消 + 修改密码（提交） -->
</template>
```

### 2.8 API 模块变更

在 `src/api/admin.js` 的 `adminAPI` 对象中新增方法：

```javascript
export const adminAPI = {
  // ... 已有方法保持不变 ...

  // 修改密码
  changePassword(currentPassword, newPassword) {
    return apiClient.put('/api/admin/password', {
      current_password: currentPassword,
      new_password: newPassword
    })
  }
}
```

---

## 3. 需求详情（逐条）

### 3.1 页面与路由

| ID | 需求 | 详细说明 |
|----|------|----------|
| PWD-FE-01 | **安全设置页面** | 新增 `AdminPassword.vue` 页面，路径 `/admin/password`，标题"管理后台 — 安全设置" |
| PWD-FE-02 | **路由配置** | 在 `AdminLayout` 子路由中新增 `AdminPassword` 路由，懒加载 |
| PWD-FE-03 | **侧边栏入口** | 在 `AdminLayout.vue` 的 `navItems` 中添加"安全设置"菜单项，图标使用 🔒（锁） |
| PWD-FE-04 | **页面标题** | 设置 `document.title` 为"管理后台 — 安全设置" |

### 3.2 表单交互

| ID | 需求 | 详细说明 |
|----|------|----------|
| PWD-FE-05 | **表单字段** | 当前密码、新密码、确认新密码三个输入框，均为密码类型 |
| PWD-FE-06 | **密码可见切换** | 每个输入框右侧有眼睛图标按钮，点击切换 `type="password"` / `type="text"` |
| PWD-FE-07 | **密码强度指示器** | 新密码输入框下方显示强度条（3 档：弱/中/强），实时更新；无输入时不显示 |
| PWD-FE-08 | **前端校验** | 提交前校验：所有字段非空、新密码 ≥ 8 位、两次密码一致；校验不通过时在输入框下方展示红色错误文字 |
| PWD-FE-09 | **提交按钮状态** | 表单任一字段为空时提交按钮置灰（disabled）；提交中按钮进入加载态、输入框全部禁用 |
| PWD-FE-10 | **取消按钮** | 点击取消返回管理后台首页 `/admin`，无需脏检测（密码表单不留脏数据） |

### 3.3 API 集成

| ID | 需求 | 详细说明 |
|----|------|----------|
| PWD-FE-11 | **API 方法** | `adminAPI.changePassword(currentPassword, newPassword)` 调用 `PUT /api/admin/password` |
| PWD-FE-12 | **成功处理** | 后端返回 200 → Toast "密码修改成功" → 表单全部重置为空 |
| PWD-FE-13 | **业务错误处理** | 后端返回 400（当前密码错误/新旧密码相同等）→ Toast 显示后端 `message` 字段，不重置表单 |
| PWD-FE-14 | **网络/服务器错误处理** | 网络超时或 500 → Toast "网络连接失败，请重试" / "服务器错误，请稍后重试" |
| PWD-FE-15 | **401 处理** | Token 过期 → 由 Axios 响应拦截器自动处理（清除 Token + 跳转登录页），页面无需额外处理 |

### 3.4 视觉与交互细节

| ID | 需求 | 详细说明 |
|----|------|----------|
| PWD-FE-16 | **UI 风格** | 与现有管理后台保持一致，使用相同的 CSS 变量（`--color-surface`、`--color-accent`、`--color-text-primary` 等） |
| PWD-FE-17 | **表单样式** | 使用现有的 `admin-form`、`admin-form__section`、`admin-form-group`、`admin-input` 类，与管理后台其他表单风格统一 |
| PWD-FE-18 | **卡片布局** | 表单置于一个 `.admin-card` 卡片中，卡片标题使用 `admin-text-page-title` 类 |
| PWD-FE-19 | **页面内联校验** | 校验失败在输入框下方显示红色文字，使用与现有 `admin-form` 一致的校验样式 |
| PWD-FE-20 | **密码框自适应宽度** | 密码框宽度与现有 `admin-input` 保持一致（约 400px），不强制全宽 |

### 3.5 页面模板示意

```vue
<template>
  <div class="admin-form">
    <h1 class="admin-text-page-title">安全设置</h1>
    <p class="admin-text-description">修改你的管理后台登录密码</p>

    <div class="admin-card" style="padding: 24px; max-width: 600px; margin-top: 24px;">
      <!-- 当前密码 -->
      <div class="admin-form-group">
        <label class="admin-form-group__label admin-form-group__label--required">当前密码</label>
        <div class="password-input-wrapper">
          <input
            v-model="form.current_password"
            :type="showPassword.current ? 'text' : 'password'"
            class="admin-input"
            placeholder="请输入当前密码"
          />
          <button class="password-toggle" @click="toggleShow('current')">
            {{ showPassword.current ? '🙈' : '👁️' }}
          </button>
        </div>
      </div>

      <!-- 新密码 -->
      <div class="admin-form-group">
        <label class="admin-form-group__label admin-form-group__label--required">新密码</label>
        <div class="password-input-wrapper">
          <input
            v-model="form.new_password"
            :type="showPassword.new ? 'text' : 'password'"
            class="admin-input"
            placeholder="至少 8 位，建议包含字母和数字"
          />
          <button class="password-toggle" @click="toggleShow('new')">
            {{ showPassword.new ? '🙈' : '👁️' }}
          </button>
        </div>
        <!-- 密码强度指示器 -->
        <div v-if="form.new_password" class="password-strength">
          <div class="password-strength__bar" :class="`password-strength__bar--${strength}`"></div>
          <span class="password-strength__label">{{ strengthText }}</span>
        </div>
        <!-- 校验提示 -->
        <p v-if="errors.new_password" class="admin-form-group__error">{{ errors.new_password }}</p>
      </div>

      <!-- 确认新密码 -->
      <div class="admin-form-group">
        <label class="admin-form-group__label admin-form-group__label--required">确认新密码</label>
        <div class="password-input-wrapper">
          <input
            v-model="form.confirm_password"
            :type="showPassword.confirm ? 'text' : 'password'"
            class="admin-input"
            placeholder="请再次输入新密码"
          />
          <button class="password-toggle" @click="toggleShow('confirm')">
            {{ showPassword.confirm ? '🙈' : '👁️' }}
          </button>
        </div>
        <p v-if="errors.confirm_password" class="admin-form-group__error">{{ errors.confirm_password }}</p>
      </div>

      <!-- 操作按钮 -->
      <div class="admin-form__actions" style="margin-top: 32px;">
        <button type="button" class="admin-btn admin-btn--secondary" @click="handleCancel">取消</button>
        <button
          type="button"
          class="admin-btn admin-btn--primary"
          :class="{ 'admin-btn--loading': submitting }"
          :disabled="submitting || !isFormValid"
          @click="handleSubmit"
        >
          修改密码
        </button>
      </div>
    </div>
  </div>
</template>
```

### 3.6 CSS 样式补充

在 `src/styles/admin.css` 中新增密码框相关样式：

```css
/* 密码输入框包装器 */
.password-input-wrapper {
  display: flex;
  align-items: center;
  gap: 8px;
  max-width: 400px;
}

.password-input-wrapper .admin-input {
  flex: 1;
}

.password-toggle {
  background: none;
  border: none;
  cursor: pointer;
  font-size: 1.2rem;
  padding: 4px;
  color: var(--color-text-secondary);
  transition: color var(--transition-base);
}

.password-toggle:hover {
  color: var(--color-text-primary);
}

/* 密码强度指示器 */
.password-strength {
  margin-top: 6px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.password-strength__bar {
  width: 120px;
  height: 4px;
  border-radius: var(--radius-sm);
  background: var(--color-border);
  transition: background var(--transition-base), width var(--transition-base);
}

.password-strength__bar--weak {
  background: var(--admin-status-danger, #ef4444);
  width: 33%;
}

.password-strength__bar--medium {
  background: var(--admin-status-warning, #f59e0b);
  width: 66%;
}

.password-strength__bar--strong {
  background: var(--admin-status-published, #22c55e);
  width: 100%;
}

.password-strength__label {
  font-size: 0.8rem;
  color: var(--color-text-secondary);
}

/* 校验错误提示 */
.admin-form-group__error {
  color: var(--admin-status-danger, #ef4444);
  font-size: 0.8rem;
  margin-top: 4px;
}
```

---

## 4. 工作量评估

| 模块 | 预估时间 |
|------|---------|
| `AdminPassword.vue` 页面组件 | 1.5 小时 |
| `adminAPI.changePassword()` API 方法 | 0.1 小时 |
| 路由配置 + 侧边栏导航更新 | 0.2 小时 |
| 密码强度指示器 + 可见切换（逻辑与样式） | 0.5 小时 |
| 表单校验 + 错误处理 + 状态管理 | 0.5 小时 |
| 测试（联调） | 0.3 小时 |
| **合计** | **~3 小时** |

---

## 5. 验证方式

| 验证项 | 方法 |
|--------|------|
| **页面访问** | 登录后访问 `/admin/password`，确认安全设置页面正常渲染 |
| **侧边栏导航** | 侧边栏出现"安全设置"菜单项（🔒 图标），点击可跳转到 `/admin/password` |
| **正常修改** | 输入正确当前密码 + 合法新密码 → 提交 → Toast "密码修改成功" → 表单重置为空 |
| **当前密码错误** | 输入错误当前密码 → 提交 → Toast 显示"当前密码错误"，表单不清空 |
| **两次密码不一致** | 新密码与确认密码不同 → 提交 → 确认密码下方显示红色错误提示，不提交 |
| **新密码太短** | 新密码 < 8 位 → 提交 → 新密码下方显示"密码至少 8 位"，不提交 |
| **密码强度反馈** | 输入新密码时，强度条实时变化（弱→中→强） |
| **密码可见切换** | 点击眼睛图标，密码在明文/密文之间切换 |
| **修改后登录验证** | 退出管理后台 → 使用新密码登录 → 成功 |
| **修改后旧密码登录** | 使用旧密码登录 → 提示"用户名或密码错误" |
| **提交按钮状态** | 表单字段为空时按钮置灰；提交中按钮呈加载态、输入框禁用 |
| **取消按钮** | 点击取消 → 返回管理后台首页 `/admin` |

---

## 6. 相关文档

- [管理后台-前端需求.md](管理后台-前端需求.md) — 现有管理后台前端需求
- [管理后台-密码修改-后端需求.md](../../python_project/xinysoft_FastAPI/管理后台-密码修改-后端需求.md) — 对应后端需求
- [项目功能迭代需求分析文档.md](项目功能迭代需求分析文档.md) — 完整功能路线图
- [管理后台-样式需求文档.md](管理后台-样式需求文档.md) — 管理后台样式规范

---

## 7. 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| 1.0 | 2026-06-12 | 初始版本，基于管理后台增量功能需求产出 |
